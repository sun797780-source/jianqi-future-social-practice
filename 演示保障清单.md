# 俭启未来演示保障清单

## 答辩前

1. 运行 `npm run check:all`，确认类型、规范和生产构建通过。
2. 启动前后端后运行 `npm run demo:check`，确认健康检查、主持页、观众页和数据文件均为 `ok`。
3. 打开 `/presenter`，确认 3D 画面、二维码和房间状态可见；用手机完成一票并确认主持端同步。
4. 明确向评审说明页面中的 `Demo 数据`、`原型演示口径`与真实核验材料的区别。
5. 若使用手机扫码，本地演示必须通过电脑局域网地址打开网页，手机与电脑保持同一网络。

## 数据备份与恢复

- 停止服务后复制 `api/data/practice-store.json` 到带日期的备份目录。不要在服务写入期间直接覆盖数据文件。
- 恢复时先保留当前文件副本，再用同一 Schema 版本的备份替换，启动后检查 `/api/health`。
- 不要用重新运行种子来“恢复”真实数据；Demo 种子只在数据文件不存在时创建。
- Serverless 无持久磁盘，正式部署前必须迁移到托管数据库。

## 断网与故障

- AI 不可用时，小俭自动使用本地主题回答，不冒充在线模型结果。
- 实践 API 暂时不可用时，公开成果页显示明确标注的 Demo 回退；观众投票页提示重连，不伪造提交成功。
- 健康检查返回 `503` 时不要继续后台写入，先恢复数据仓储，再重新运行 `npm run demo:check`。

## 回滚

1. 保留当前构建产物、环境变量配置和数据备份。
2. 回滚应用代码或部署版本，不回滚或覆盖实践数据文件。
3. 启动旧版本前确认它支持当前仓储 Schema；不兼容时先在副本上验证迁移。
4. 回滚后依次检查健康接口、主持页、观众页、后台登录和一条只读证据记录。
