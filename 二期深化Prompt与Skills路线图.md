# 俭启未来二期深化 Prompt 与 Skills 路线图

## 当前网页实测结论

- 首页桌面端具有完整展示感，3D 文明核心、主标题和四时代入口层级清晰。
- 手机端首页可用，首屏信息较长但无明显横向溢出；3D 与小俭会占据较多纵向空间，后续继续控制性能和遮挡。
- 成果页在手机端能展示指标、地图和证据，但地图标签需要限制宽度，避免靠边溢出。
- 内容后台第一版功能可用；本轮已修复浅色主题下列表和表单对比度不足，并从公共导航移除后台入口。
- 当前最大短板已经从“页面空壳”转为“数据、权限、实时同步和真实证据素材仍是原型”。

## 二期总目标

把当前高完成度演示原型升级为可持续维护、可真实录入、可现场稳定使用的社会实践展示系统，同时继续明确区分真实数据、核验数据和 Demo 数据。

## 25 数据持久化与仓储层

**Prompt**：

> 使用 `$jianqi-content-persistence` 将证据条目、影响指标、行动承诺、互动房间和投票从进程内存迁移到可持久化仓储。优先采用适合本项目部署方式的轻量方案，建立 typed repository、迁移、演示种子和备份边界。服务重启后数据必须保留；公开接口只返回已发布内容；Demo 种子必须明确标注，不得混入真实统计。

**Skill**：`jianqi-content-persistence`

**验收**：编辑证据和投票后重启服务，数据仍存在；迁移和种子初始化可重复执行且不会静默覆盖已有数据。

## 26 正式管理员登录与权限

**Prompt**：

> 使用 `$jianqi-admin-auth` 替换共享 `X-Admin-Key`。实现管理员登录、会话过期、角色校验、退出、写操作审计和归档恢复。权限必须在 Express API 边界强制执行；前端提供登录失败、会话失效和无权限状态；不得把密码、令牌或密钥写入前端代码和日志。

**Skill**：`jianqi-admin-auth`

**验收**：未登录不能读取管理数据；编辑者可修改草稿但不能执行超出角色的操作；每次发布和归档都有操作人及时间记录。

## 27 实时互动房间

**Prompt**：

> 使用 `$jianqi-realtime-room` 将当前 3 秒轮询升级为可恢复的实时房间。设计等待、直播、暂停和关闭状态；支持匿名投票、重复提交防护、断线重连、主持端结果同步和 Demo 降级。二维码应指向可加入的观众页面，而不是仅展示图标。

**Skill**：`jianqi-realtime-room`

**验收**：两台设备加入同一房间，投票结果在主持端及时同步；重复投票不异常累加；关闭房间后拒绝新投票。

## 28 观众端互动页面

**Prompt**：

> 为 `/join/:roomId` 设计移动端优先的观众页面，包含房间状态、单选投票、提交反馈、已投票回执、结果是否公开和断线恢复。页面不要求注册，不采集姓名、手机号或精确位置；无效房间和活动结束要有明确下一步。

**Skills**：`jianqi-realtime-room`、`jianqi-responsive-system`、`jianqi-motion-accessibility`

**验收**：手机扫码后 3 步内完成投票；键盘和屏幕阅读器可完成同样流程；弱网时不会重复提交。

## 29 真实实践素材工作流

**Prompt**：

> 使用 `$jianqi-evidence-media` 为真实照片、访谈音频、短视频和扫描材料建立上传前处理流程。记录来源、日期、授权、版权和核验状态；移除敏感元数据，对未经授权的人脸和身份信息进行处理；生成响应式图片、视频封面、替代文本和低带宽回退。

**Skill**：`jianqi-evidence-media`

**验收**：每个公开素材都能追溯来源和授权；移动端加载不明显跳动；缺失素材不会被伪装成真实证据。

## 30 后台素材与证据编辑器

**Prompt**：

> 扩展内容后台，使管理员可以新增证据、关联时代、选择证据类型、填写来源和核验状态、关联已批准媒体、预览公开卡片、保存草稿、发布、归档和恢复。加入字段校验、未保存提醒、冲突提示和保存失败恢复。

**Skills**：`jianqi-content-admin`、`jianqi-evidence-media`、`jianqi-admin-auth`

**验收**：不修改代码即可创建一条带图片的证据并在公开成果页预览；归档后可恢复且审计记录完整。

## 31 浏览器视觉回归

**Prompt**：

> 使用 `$jianqi-browser-qa` 对首页、时代长廊、时代详情、互动行动、成果页、宣讲页和后台页进行桌面与 390px 手机视觉验收。覆盖浅色主题、导航、3D Canvas、桌宠遮挡、键盘焦点、低特效、加载、错误和 Demo 降级。每次发现问题必须记录路由、视口和可复现步骤。

**Skill**：`jianqi-browser-qa`

**验收**：核心路由无横向溢出、文字截断、低对比度、空白 Canvas 或不可操作控件；检查过程可重复执行。

## 32 可观测性与演示保障

**Prompt**：

> 使用 `$jianqi-observability` 增加依赖健康检查、请求编号、隐私安全错误日志、API 延迟、3D 初始化耗时和演示环境诊断。补充演示前检查清单、数据库备份、种子数据初始化、服务异常回退和回滚说明。不得记录完整聊天内容、令牌或个人信息。

**Skill**：`jianqi-observability`

**验收**：依赖故障能被健康检查发现；日志能定位请求但不含敏感信息；答辩现场断网时仍可使用明确标注的本地演示模式。

## 推荐开发顺序

1. 25 数据持久化与仓储层
2. 26 正式管理员登录与权限
3. 30 后台素材与证据编辑器
4. 29 真实实践素材工作流
5. 27 实时互动房间
6. 28 观众端互动页面
7. 31 浏览器视觉回归
8. 32 可观测性与演示保障

## 本轮已完成

- [x] 实测首页、成果页和内容后台的桌面/手机效果。
- [x] 修复浅色主题后台文字和表单对比度。
- [x] 限制移动端成果地图标签宽度。
- [x] 从公开导航移除内容后台入口，保留 `/admin` 直达路径。
- [x] 创建二期 6 个独立可调用 Skills。
- [x] 整理 25 至 32 项深化 Prompt、验收和开发顺序。
- [x] 25 数据持久化与仓储层第一版：证据、承诺、互动房间和投票已迁移到版本化原子 JSON 仓储；完成重启保留、幂等、公开过滤和损坏文件 `503` 验证。
- [x] 26 正式管理员登录与权限第一版：已替换前端共享 Key，加入 HttpOnly 会话、CSRF、登录限流、会话过期、退出、服务端操作身份和持久化审计记录。
- [x] 27 实时互动房间第一版：主持端已使用 SSE 实时同步，房间具备等待、直播、暂停、关闭状态；匿名参与标识会哈希保存，重复投票不累加，关闭后拒绝写入。
- [x] 27 主持端浏览器验收：修复小俭遮挡二维码按钮；二维码已替换为可扫描的 `/join/:roomId` 实际入口；主持端只展示实时结果，并将固定人数替换为明确标注的房间 Demo 票数。
- [x] 28 观众端互动页面第一版：新增移动优先的 `/join/:roomId`，完成房间状态、匿名单选、提交回执、提交后结果、重复投票提示、断线重连和无效房间恢复页。
- [x] 28 联调验收：390px 与 320px 无横向溢出；首票使主持端从事件 1/38 票同步到事件 2/39 票；刷新后重复提交未增加票数；TypeScript、ESLint 与生产构建全部通过。
- [x] 31 浏览器视觉回归第一轮：首页、时代长廊、时代详情、互动行动、成果页、宣讲页和后台页均完成桌面与 390px 扫描，核心页面无横向溢出；全新页面控制台无报错或警告。
- [x] 31 回归修复：时代详情补入对应时代的无边框 3D 舞台与可访问描述；缩小小俭关闭态并将提示改为悬停显示；手机宣讲控制条移至章节列表前，二维码可在首屏下方直接操作。
- [x] 32 可观测性与演示保障第一版：健康接口新增服务、仓储、AI 和管理员配置状态；API 增加请求编号、耗时与隐私安全结构化日志；前端记录路由、API 和 Canvas 本地性能指标。
- [x] 32 演示自检与恢复：新增 `npm run demo:check`，健康、主持页、观众页和数据文件四项通过；新增答辩前检查、备份、断网降级和不覆盖实践数据的回滚清单。
- [x] 32 体验补强：确认首页、时代详情和主持页的 Canvas 持续动画；修复小俭本地回答的实际交互验收；语音优先选择 Xiaoxiao/Xiaoyi 等清脆中文女声，并在不支持语音的浏览器中明确提示文字模式。
- [x] 小俭形象重设计：保留 React Three Fiber 3D 模型，统一青绿色未来讲解员视觉，增强脸部识别、侧马尾、服装层次、金色俭字徽章与状态动画；桌宠小尺寸下提升可读性。
- [x] 小俭形象第二版：撤回不够统一的长发少女方案，改为圆润青绿色节约能量体，采用叶片头饰、明亮大眼、金色徽章和更强轮廓，强化桌宠缩小状态下的辨识度。
- [x] 四时代动画片播放器：时代详情页新增自动播放的时代叙事动画，按时代切换农田、奋斗建设、绿色校园和未来城市画面，提供章节字幕、暂停/继续、进度拖动、重播与移动端控制。
- [x] 阅读字号优化：提高首页说明、时代卡片、时代详情正文、动画字幕、任务说明、行动卡片、成果证据和小俭对话字号；保留编号、英文标识与状态标签的小字号层级。
- [x] 首页文明叙事标题字号二次优化：针对“四幕文明叙事”和“每个时代，都在回答同一个问题”区域进一步放大主标题、栏目标题、说明文字与时代卡片正文，并单独适配手机端。
