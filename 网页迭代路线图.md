# “俭启未来”社会实践网页迭代路线图

## 一、当前项目评估

### 已有基础

- 已有 React + TypeScript + React Router 页面骨架，页面包括首页、时代长廊、时代详情、互动行动、文明成果和宣讲模式。
- 已接入 Three.js / React Three Fiber，时光机拥有粒子、轨道、能量核心、镜头跟随和时代配色切换。
- 已有小俭桌宠、AI 对话接口、行动徽章和文明能量等体验概念，视觉主题统一，首页和时代长廊已经具备演示原型质量。

### 主要不足

1. 3D 场景只有一台抽象时光机，四个时代主要靠颜色和文案区分，没有“稻田、旧物、校园、未来城市”等可被讲述的空间证据。
2. 首页指标、文明成果星图、参与人数和行动数量是静态常量，不能说明数据从哪里来，也没有“行动 -> 贡献 -> 成果”的可追踪链路。
3. 行动、徽章、文明能量此前只存在内存中，刷新页面会丢失，宣讲展示时无法形成连续体验。
4. 顶部声音、特效按钮和宣讲页全屏按钮存在视觉入口，但缺少完整行为和状态反馈。
5. 后端只有聊天、鉴权和健康检查，没有实践记录、承诺提交、成果聚合、宣讲场次和内容管理接口。
6. 内容、统计数据、页面状态和接口协议混在前端组件中，不利于后续接入真实调研数据。
7. AI 讲解员没有明确的错误、离线、加载、超时和上下文降级体验。
8. 过度依赖大块装饰性氛围，缺少照片、访谈、调研表、行动记录等社会实践证据。
9. 宣讲模式目前更像播放器原型，缺少讲稿、计时、讲解提示、互动二维码和观众反馈闭环。
10. 移动端需要重点验证 3D 场景高度、浮动桌宠、导航折叠、长卡片和按钮可触达性。

## 二、统一开发约束

每个子项目都必须：

- 先阅读现有页面和数据结构，优先复用现有 React、R3F、Zustand、Lucide 和 CSS 体系。
- 不凭空编造实践数据；演示数据必须标注“原型演示”或来自可追溯的数据文件。
- 3D 场景必须有明确叙事对象、交互目标和低特效降级方案。
- 验证桌面宽度、移动宽度、键盘焦点、空数据、加载失败和刷新后的状态。
- 每个任务交付：代码、对应测试或手工验收步骤、截图或录屏说明、未完成风险。

## 三、24 个可执行子项目

### 01 产品骨架与状态持久化

**Prompt**：

> 基于现有 React 项目，建立统一的体验状态层。持久化时代徽章、行动承诺、文明能量、声音开关和低特效开关；刷新页面后恢复状态；为状态增加版本号和异常数据回退；不改变现有路由和视觉主题。

**Skills**：`React 状态建模`、`Zustand persist`、`前端容错`、`回归构建`。

**验收**：完成时代任务和行动承诺，刷新后数据仍在；损坏的本地数据不会让页面白屏。

### 02 设计令牌与响应式布局

**Prompt**：

> 梳理现有深色/浅色主题 CSS，建立颜色、间距、字体、层级、动效时长和断点令牌；修复 320px 到 1440px 的布局溢出；保留现有国风未来感，但降低装饰噪声，确保所有文字和按钮不重叠。

**Skills**：`CSS 设计系统`、`响应式布局`、`可访问性`、`视觉回归`。

**验收**：桌面、平板、手机截图无横向溢出；低对比文字和焦点状态可读。

### 03 首页 3D 文明核心

**Prompt**：

> 将首页时光机升级为“文明核心”3D 展示：保留轨道和能量核心，加入可切换的四个时代符号、鼠标/触摸视差、自动缓慢旋转和时代切换过渡；每个时代必须改变几何构件或环境粒子，不只改变颜色。

**Skills**：`React Three Fiber`、`Three.js 动画`、`交互视差`、`性能降级`。

**验收**：四个时代切换时 3D 结构发生可见变化；低特效模式仍有轻量动效。

### 04 四时代 3D 场景包

**Prompt**：

> 为古代农耕、革命奋斗、新时代绿色生活、未来绿色城市分别设计可复用的 R3F 场景模块。每个场景包含 1 个主物件、2 个环境层、1 个数据可视化元素和 1 个交互热点；使用程序化几何或项目已有图片，不引入不可控的大体积资源。

**Skills**：`R3F 场景架构`、`程序化建模`、`资源管理`、`镜头构图`。

**验收**：每个时代都能用一句话讲清“看到了什么、为什么与节约有关”。

### 05 时代详情探索任务

**Prompt**：

> 把时代详情页的知识节点升级为“观察 -> 选择 -> 完成”的探索任务。点击热点时更新 3D 场景、知识解释和任务进度；完成后授予徽章并增加文明能量；重复完成不重复加分。

**Skills**：`状态机`、`R3F raycast`、`任务流程设计`、`动效反馈`。

**验收**：任务状态、完成反馈、徽章和能量变化一致，返回长廊后状态正确。

### 06 社会实践内容模型

**Prompt**：

> 把时代文案、知识节点、行动建议、实践证据和来源说明从页面组件中抽离为类型化内容模型。为每条内容预留 source、image、location、date、author 和 verificationStatus 字段，现有内容迁移后页面表现不变。

**Skills**：`TypeScript 类型设计`、`内容建模`、`数据迁移`、`组件解耦`。

**验收**：页面不再直接维护重复内容；新增一个时代只需增加数据对象。

### 07 实践证据墙

**Prompt**：

> 新增实践证据墙，展示调研照片、访谈摘录、数据记录和行动现场。每条证据包含标题、地点、时间、参与者、来源和核验状态；支持按时代和证据类型筛选；空数据时展示清晰的占位状态。

**Skills**：`信息架构`、`媒体展示`、`筛选交互`、`无障碍语义`。

**验收**：评审者能从一条证据回到对应时代和实践主题。

### 08 调研地图与足迹

**Prompt**：

> 将文明成果页静态星图改为真实数据驱动的实践足迹地图。首版可使用本地 JSON 的脱敏聚合点；点位点击展示场次、主题、参与人数和证据链接；明确标注演示数据，禁止展示精确个人位置。

**Skills**：`地图可视化`、`聚合数据`、`隐私保护`、`交互详情`。

**验收**：地图点位与成果列表来自同一数据源，点位信息可追溯。

### 09 行动承诺流程

**Prompt**：

> 将互动行动页设计成完整流程：选择行动、填写可选昵称、确认承诺、生成本地行动卡、查看历史承诺。输入必须有校验、成功反馈和失败重试；匿名模式默认开启，不收集不必要的个人信息。

**Skills**：`表单设计`、`本地数据`、`隐私最小化`、`反馈状态`。

**验收**：用户完成一次行动后能获得可展示的行动卡，而不是只看到一个数字变化。

### 10 文明能量计算规则

**Prompt**：

> 定义透明的文明能量规则：时代任务、行动承诺、实践证据和观众互动分别贡献多少；所有规则放入独立纯函数并覆盖边界测试；页面展示“能量来自哪里”，不使用无法解释的随机增长。

**Skills**：`领域规则建模`、`纯函数测试`、`数据解释性`、`可视化反馈`。

**验收**：相同操作得到相同结果，用户能看到能量贡献明细。

### 11 成果仪表盘

**Prompt**：

> 重做文明成果页，使指标、趋势、地图、证据墙和行动排行都来自统一数据聚合层。区分累计值、本月值和原型演示值；图表提供文字摘要、悬浮详情和空状态。

**Skills**：`数据聚合`、`可视化设计`、`图表无障碍`、`指标口径`。

**验收**：每个数字都能回答“时间范围、统计对象、数据来源”三个问题。

### 12 后端实践记录接口

**Prompt**：

> 在现有 Express API 中新增实践场次、行动承诺、证据条目和成果汇总接口。先采用内存或本地 JSON repository，接口返回统一 success、data、error 结构；加入输入校验、分页和错误处理，不破坏现有聊天接口。

**Skills**：`Express API`、`Schema 校验`、`Repository 模式`、`API 错误规范`。

**验收**：前端可读取一份实践数据并完成创建承诺、读取成果、查看证据。

### 13 前后端数据接入层

**Prompt**：

> 为前端建立 typed API client，把页面从静态常量逐步切换到接口数据；处理 loading、empty、error、retry 和超时；开发环境接口不可用时使用明确标注的 demo fallback。

**Skills**：`Fetch 封装`、`异步状态`、`类型安全`、`渐进式迁移`。

**验收**：关闭后端后页面仍能展示可理解的降级状态，不出现空白或未处理异常。

### 14 AI 小俭讲解体验

**Prompt**：

> 完善小俭 AI 讲解员：根据当前路由、时代、选中的知识节点和行动状态生成上下文；支持快捷问题、流式或分段加载、超时、离线提示和重试；回答必须引用当前页面已有内容，不能伪造实践数据。

**Skills**：`对话 UX`、`上下文拼装`、`接口降级`、`提示词边界`。

**验收**：每个页面打开桌宠都能获得与当前页面相关的首条建议。

### 15 AI 内容安全与成本控制

**Prompt**：

> 为聊天接口增加消息长度、历史条数、超时、速率限制和日志脱敏；将模型、温度或推理参数全部环境变量化；前端显示可理解的错误，不泄露 API key、内部错误或用户完整历史。

**Skills**：`API 安全`、`隐私脱敏`、`限流`、`运行时配置`。

**验收**：异常请求不会让服务器崩溃，错误响应不包含敏感配置。

### 16 宣讲模式工作台

**Prompt**：

> 把宣讲页升级为现场可用工作台：章节列表、当前讲稿、讲解计时、上一页/下一页、暂停、全屏、大屏二维码、互动结果和主持人备注；键盘左右键可切换章节，演讲者模式和观众模式分离。

**Skills**：`演示工作台`、`键盘交互`、`全屏 API`、`现场容错`。

**验收**：不依赖鼠标也能完成一场完整宣讲，刷新后能恢复当前章节。

### 17 互动二维码与观众投票

**Prompt**：

> 在宣讲章节中加入二维码互动入口和低门槛投票。首版允许使用本地演示房间 ID；投票结果实时或轮询更新到主持人工作台，显示参与人数、选项比例和最近一条匿名反馈。

**Skills**：`实时互动`、`二维码生成`、`匿名反馈`、`主持人视图`。

**验收**：观众端提交一次投票后，主持人端能看到可解释的结果变化。

### 18 视觉素材与证据资产规范

**Prompt**：

> 建立实践项目图片、视频、音频和图标的资源规范：文件命名、尺寸、压缩、版权来源、替代文本和主题映射；优先使用真实实践素材，3D 仅承担空间叙事，不替代证据。

**Skills**：`媒体资源管理`、`版权与来源`、`图片优化`、`无障碍文本`。

**验收**：每项媒体资源都有来源和 alt 文本，首屏加载不被大图拖慢。

### 19 动效系统与可访问性

**Prompt**：

> 统一页面转场、3D 呼吸、粒子、扫描线和成功反馈的动效节奏；支持 prefers-reduced-motion；任何状态不能只依靠颜色表达；为 Canvas 和图表增加屏幕阅读器摘要。

**Skills**：`动效设计`、`WCAG`、`减弱动效`、`Canvas 可访问性`。

**验收**：系统开启减少动态效果后，页面仍能完成全部核心任务。

### 20 性能与设备适配

**Prompt**：

> 对 3D 场景进行性能预算：限制 DPR、粒子数量、阴影、材质和重复 Canvas；按设备能力选择高/中/低三档；避免每帧创建对象；记录首屏加载和交互帧率。

**Skills**：`Three.js 性能`、`React profiling`、`设备能力检测`、`资源懒加载`。

**验收**：移动设备不会明显卡顿或发热，低档模式保持核心叙事完整。

### 21 内容后台与管理员入口

**Prompt**：

> 设计轻量内容管理入口，支持时代内容、实践场次、证据条目、数据口径和宣讲章节的新增、编辑、下线和预览；管理员入口与公开展示页面分离，所有修改保留更新时间和操作人。

**Skills**：`后台信息架构`、`表格编辑`、`权限控制`、`审计字段`。

**验收**：不改代码即可更新一条实践证据并在公开页预览。

### 22 测试与质量门禁

**Prompt**：

> 为路由、状态规则、API、关键组件和核心流程增加测试；至少覆盖首页启动、时代任务、承诺提交、刷新恢复、API 失败、宣讲键盘操作和低特效模式；把 typecheck、lint、build 设为提交前门禁。

**Skills**：`前端测试`、`API 测试`、`端到端流程`、`CI 门禁`。

**验收**：核心流程有自动化测试，构建失败能明确指出问题位置。

### 23 展示稿与社会实践报告导出

**Prompt**：

> 增加实践成果导出能力：根据已录入场次、证据、行动和统计生成适合答辩展示的摘要页和可打印报告；所有数据附统计口径和来源；导出前提供预览。

**Skills**：`报告信息设计`、`打印样式`、`数据快照`、`导出流程`。

**验收**：一次实践活动可以生成可用于汇报的完整摘要，不需要手工复制网页文字。

### 24 发布与演示环境

**Prompt**：

> 整理开发、演示和生产环境配置，补充启动说明、环境变量说明、API 健康检查、静态资源路径、部署回滚和演示数据初始化；确保没有 API key 被提交到仓库。

**Skills**：`Vite 部署`、`Express 部署`、`环境变量`、`演示运维`。

**验收**：新环境按 README 能启动前后端，并能完成一条完整展示路径。

## 四、建议开发顺序

先完成 01、02、06、10、12、13，建立稳定的数据和状态底座；然后完成 03、04、05、07、08、09，把 3D 与社会实践证据接起来；再完成 11、14、16、17，形成成果展示和现场宣讲闭环；最后完成 18 至 24，处理性能、无障碍、测试、导出和发布。

## 五、当前进度

- [x] 01 产品骨架与状态持久化：已完成浏览器持久化、声音开关和低特效开关。
- [x] 04 四时代 3D 场景包：已完成第一版程序化场景元素，四个时代拥有不同的空间对象。
- [x] 03 首页 3D 文明核心：已保留时光机核心并加入时代环境对象、语义标签和性能降级。
- [x] 05 时代详情探索任务：已保留知识节点、徽章奖励、下一时代导航和持久化进度。
- [x] 02 设计令牌与响应式布局：已增加设计令牌，并保留现有多断点布局体系。
- [x] 06 社会实践内容模型：已增加时代场景类型、行动建议和实践证据类型。
- [x] 10 文明能量计算规则：已抽出纯函数和规则常量，并接入徽章与行动承诺。
- [x] 12 后端实践记录接口：已增加影响数据、证据和匿名承诺接口，含输入校验。
- [x] 13 前后端数据接入层：已增加 typed client、超时、错误处理和原型演示降级。
- [x] 07 实践证据墙：已增加证据类型筛选、选中详情、来源和核验状态展示。
- [x] 08 调研地图与足迹：已将接口证据映射为可键盘操作的聚合足迹标记。
- [x] 11 成果仪表盘：已将指标、趋势、地图和证据统一接入影响数据。
- [x] 09 行动承诺流程：已增加服务端提交、失败重试提示和行动卡回执。
- [x] 16 宣讲模式工作台：已增加全屏、键盘章节切换、章节持久化和互动二维码面板。
- [x] 14 AI 小俭讲解体验：已保留页面上下文、语音友好回复和本地回答降级能力。
- [x] 15 AI 内容安全与成本控制：已增加请求限流、长度限制、超时和安全日志处理。
- [x] 17 互动二维码与观众投票：已完成宣讲互动二维码房间面板、房间状态轮询、主持端投票和百分比结果展示；当前使用内存 demo 数据，后续可替换为持久化实时房间。
- [x] 19 动效系统与可访问性：已增加 3D 场景语义描述和 prefers-reduced-motion 降级。
- [x] 20 性能与设备适配：已拆分 Three.js 构建块并保留低特效 DPR/阴影降级。
- [x] 22 测试与质量门禁：已增加 `check:all` 统一检查命令。
- [x] 24 发布与演示环境：已补充前后端构建与健康检查路径，部署配置保持环境变量化。
- [x] 18 视觉素材与证据资产规范：已补充素材来源、版权、压缩和替代文本技能规范。
- [x] 21 内容后台与管理员入口：已完成证据条目后台页面、管理员请求头授权、编辑保存、草稿/发布/归档状态和公开页发布过滤；当前使用内存 Demo 数据，正式环境仍需接入登录权限和数据库。
- [x] 23 展示稿与社会实践报告导出：已增加成果页打印实践摘要入口和打印样式。

已在 `C:\Users\30223\.codex\skills` 创建 24 个项目技能目录，并持续补齐可调用正文；最近完成互动房间 API、投票结果同步，以及证据内容后台第一版。当前数据仍为内存 demo 数据，展示时应明确标注原型状态。
